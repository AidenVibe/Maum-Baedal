# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local-only tools and test UIs
.claude/
.claude.json
Claude_Optiomize_Guide/
SuperClaude/
app/test/
app/dev/
components/dev/
scripts/local-*/
*.local.*

# Windows reserved device filenames that can appear accidentally
# Prevents commit failures like "fatal: adding files failed" on Windows
nul

# Local admin tooling (not pushed)
app/admin/

# Local cookies/session dumps
cookies*.txt

# Internal docs with example secrets (excluded from repo)
SOLAPI_GUIDE.md
FEATURES/deployment-strategy.plan.md
